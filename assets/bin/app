#!/bin/bash
# app - The TrashcanOS Package Manager Wrapper
# 🍌 TrashcanOS: Chaos Verified.

COMMAND="$1"
TARGET="$2"
CONTAINER_NAME="trashcan-box"

# Colors
GREEN='\033[0;32m'
BLUE='\033[0;34m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
NC='\033[0m' # No Color

# Helper: Ensure the backend container exists
ensure_container() {
    if ! podman container exists "$CONTAINER_NAME" > /dev/null 2>&1; then
        echo -e "${GREEN}📦 Initializing the App Environment (First run only)...${NC}"
        # Create a Fedora container silently
        distrobox create -n "$CONTAINER_NAME" -i registry.fedoraproject.org/fedora:latest -Y > /dev/null
    fi
}

show_help() {
    echo -e "🗑️  ${GREEN}TrashcanOS App Manager${NC}"
    echo "Usage:"
    echo "  app install <package|file>   Install an app (rpm, flatpak, or repo name)"
    echo "  app remove <package>         Remove an app"
    echo "  app update [package]         Update apps only (DNF)"
    echo "  app upgrade [package]        Update System (Bootc) + Apps (DNF)"
    echo "  app full-upgrade             Force update EVERYTHING (Flatpak, DNF, Bootc, RPM-OSTree)"
    echo "  app list                     List installed apps"
}

if [ -z "$COMMAND" ]; then
    show_help
    exit 1
fi

case "$COMMAND" in
    install)
        if [ -z "$TARGET" ]; then
            echo -e "${RED}Error: You must specify a package name or file.${NC}"
            exit 1
        fi

        # --- SMART CHECK: Is it already installed? ---
        ensure_container
        if distrobox enter "$CONTAINER_NAME" -- rpm -q "$TARGET" > /dev/null 2>&1; then
            echo -e "${YELLOW}⚠️  '$TARGET' is already installed.${NC}"
            echo -e "Did you mean to update it? Run: ${GREEN}app update $TARGET${NC}"
            exit 0
        fi
        # ---------------------------------------------

        # --- 1. HANDLE FLATPAKS (Host Level) ---
        if [[ "$TARGET" == *.flatpakref ]] || [[ "$TARGET" == *.flatpak ]]; then
             echo -e "${GREEN}📦 Detected Flatpak file. Installing to System (User)...${NC}"
             flatpak install -y "$TARGET"
             echo -e "${GREEN}✅ Flatpak installed!${NC}"
             exit 0
        fi

        # --- 2. HANDLE EVERYTHING ELSE (Container Level) ---
        ensure_container

        # Check: Is it a local .rpm file?
        if [[ "$TARGET" == *.rpm ]]; then
            if [ -f "$TARGET" ]; then
                echo -e "${GREEN}📦 Detected local RPM file. Installing in container...${NC}"
                RPM_PATH=$(realpath "$TARGET")
                distrobox enter "$CONTAINER_NAME" -- sudo dnf install -y "$RPM_PATH"
            else
                echo -e "${RED}Error: File '$TARGET' not found.${NC}"
                exit 1
            fi
        else
            # Standard repo package
            echo -e "${GREEN}⬇️  Downloading and installing $TARGET...${NC}"
            distrobox enter "$CONTAINER_NAME" -- sudo dnf install -y "$TARGET"
        fi

        # Export to menu
        echo -e "${GREEN}🔗 Exporting to host menu...${NC}"
        distrobox enter "$CONTAINER_NAME" -- distrobox-export --export-app "$TARGET" 2>/dev/null || true
        # 🗑️ REMOVED THE INVALID FLAG HERE 🗑️

        echo -e "${GREEN}✅ Installation complete!${NC}"
        ;;

    remove)
        if [ -z "$TARGET" ]; then
            echo -e "${RED}Error: What do you want to remove?${NC}"
            exit 1
        fi
        ensure_container
        distrobox enter "$CONTAINER_NAME" -- sudo dnf remove -y "$TARGET"
        echo -e "${GREEN}🗑️  $TARGET removed.${NC}"
        ;;

    update)
        ensure_container
        if [ -z "$TARGET" ]; then
            # No argument: Update ALL Apps
            echo -e "${GREEN}📦 Updating all Container Apps...${NC}"
            distrobox enter "$CONTAINER_NAME" -- sudo dnf upgrade --refresh -y
            # Note: No need to re-export apps, dnf handles the binary updates in place.
            echo -e "${GREEN}✅ App updates complete.${NC}"
        else
            # Argument provided: Update specific App
            echo -e "${GREEN}📦 Updating package: $TARGET...${NC}"
            distrobox enter "$CONTAINER_NAME" -- sudo dnf upgrade --refresh -y "$TARGET"
            echo -e "${GREEN}✅ Update complete.${NC}"
        fi
        ;;

    upgrade)
        # 1. Update Apps
        ensure_container
        if [ -z "$TARGET" ]; then
            echo -e "${BLUE}============ 1/2: UPDATING APPS ============${NC}"
            distrobox enter "$CONTAINER_NAME" -- sudo dnf upgrade --refresh -y

            echo -e "${BLUE}============ 2/2: UPDATING SYSTEM (Bootc) ============${NC}"
            echo -e "${YELLOW}Checking for TrashcanOS Core Updates...${NC}"
            sudo /usr/bin/bootc upgrade

            echo -e "${GREEN}✅ System & App Upgrade Complete.${NC}"
        else
            # If user specifies a package, just update that package
            echo -e "${GREEN}📦 Updating package: $TARGET...${NC}"
            distrobox enter "$CONTAINER_NAME" -- sudo dnf upgrade --refresh -y "$TARGET"
        fi
        ;;

    full-upgrade)
        if [ ! -z "$TARGET" ]; then
             echo -e "${RED}Error: 'full-upgrade' does not accept package names. It updates EVERYTHING.${NC}"
             exit 1
        fi

        echo -e "${RED}⚠️ WARNING: Performing a FULL SYSTEM overhaul${NC}"

        # 1. Flatpaks
        echo -e "${BLUE}📦 [1/4] Updating Flatpaks...${NC}"
        flatpak update -y

        # 2. Containers
        echo -e "${BLUE}🧊 [2/4] Updating Container Apps...${NC}"
        ensure_container
        distrobox enter "$CONTAINER_NAME" -- sudo dnf upgrade --refresh -y

        # 3. System Base (Bootc)
        echo -e "${BLUE}💿 [3/4] Updating TrashcanOS Core...${NC}"
        sudo /usr/bin/bootc upgrade

        # 4. Layered Packages (RPM-OSTree)
        echo -e "${BLUE}🛡️  [4/4] Updating Layered Drivers...${NC}"
        sudo /usr/bin/rpm-ostree upgrade

        echo -e "${GREEN}✅ FULL SYSTEM UPGRADE COMPLETE!${NC}"
        echo -e "${YELLOW}ℹ️  Please reboot if System/Core updates were applied.${NC}"
        ;;

    list)
        ensure_container
        distrobox enter "$CONTAINER_NAME" -- dnf list installed --userinstalled
        ;;

    *)
        show_help
        ;;
esac
