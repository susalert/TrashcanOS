#!/bin/bash
# app - The TrashcanOS Package Manager Wrapper

COMMAND="$1"
PACKAGE="$2"
CONTAINER_NAME="trashcan-box"

# Colors
GREEN='\033[0;32m'
RED='\033[0;31m'
NC='\033[0m' # No Color

show_help() {
    echo -e "🗑️  ${GREEN}TrashcanOS App Manager${NC}"
    echo "Usage:"
    echo "  app install <package>   Install an app (e.g., firefox, vlc)"
    echo "  app remove <package>    Remove an app"
    echo "  app list                List installed apps"
}

if [ -z "$COMMAND" ]; then
    show_help
    exit 1
fi

# Ensure the backend container exists
if ! podman container exists "$CONTAINER_NAME" > /dev/null 2>&1; then
    echo -e "${GREEN}📦 Initializing the App Environment (First run only)...${NC}"
    # Create a Fedora container silently
    distrobox create -n "$CONTAINER_NAME" -i registry.fedoraproject.org/fedora:latest -Y > /dev/null
fi

case "$COMMAND" in
    install)
        if [ -z "$TARGET" ]; then
            echo -e "${RED}Error: You must specify a package name or .rpm file.${NC}"
            exit 1
        fi

        ensure_container

        # CHECK: Is it a local .rpm file?
        if [[ "$TARGET" == *.rpm ]]; then
            # It is an RPM file!
            if [ -f "$TARGET" ]; then
                echo -e "${GREEN}📦 Detected local RPM file. Installing in container...${NC}"

                # Get absolute path.
                # NOTE: This works best if the file is inside your Home folder (Downloads, etc),
                # because Distrobox automatically mounts Home.
                RPM_PATH=$(realpath "$TARGET")

                # Install inside the container (No reboot needed!)
                distrobox enter "$CONTAINER_NAME" -- sudo dnf install -y "$RPM_PATH"
            else
                echo -e "${RED}Error: File '$TARGET' not found.${NC}"
                exit 1
            fi
        else
            # It is a standard repo package
            echo -e "${GREEN}⬇️  Downloading and installing $TARGET...${NC}"
            distrobox enter "$CONTAINER_NAME" -- sudo dnf install -y "$TARGET"
        fi

        # Export the app so it shows up in the menu
        echo -e "${GREEN}🔗 Exporting to host menu...${NC}"
        # We try to export the specific target first
        distrobox enter "$CONTAINER_NAME" -- distrobox-export --export-app "$TARGET" 2>/dev/null || true
        # Then we force a full export scan to catch any weird binary names
        distrobox enter "$CONTAINER_NAME" -- distrobox-export --export-apps

        echo -e "${GREEN}✅ Installation complete!${NC}"
        ;;

    remove)
        if [ -z "$PACKAGE" ]; then
            echo -e "${RED}Error: What do you want to remove?${NC}"
            exit 1
        fi
        distrobox enter "$CONTAINER_NAME" -- sudo dnf remove -y "$PACKAGE"
        echo -e "${GREEN}🗑️  $PACKAGE removed.${NC}"
        ;;

    update)
        echo -e "${GREEN}🔄 Checking for updates...${NC}"
        # Just refresh metadata, don't install yet
        distrobox enter "$CONTAINER_NAME" -- sudo dnf check-update
        ;;

    upgrade)
        echo -e "${GREEN}⬆️  Upgrading all apps...${NC}"
        # Run the actual upgrade
        distrobox enter "$CONTAINER_NAME" -- sudo dnf upgrade -y
        # Refresh menu icons in case they changed
        echo -e "${GREEN}🔗 Refreshing menu links...${NC}"
        distrobox enter "$CONTAINER_NAME" -- distrobox-export --export-apps
        echo -e "${GREEN}✅ Upgrade complete.${NC}"
        ;;

    list)
        distrobox enter "$CONTAINER_NAME" -- dnf list installed --userinstalled
        ;;

    *)
        show_help
        ;;
esac
